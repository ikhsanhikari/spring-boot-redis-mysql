package id.hikari.hikari.test.dao;

import id.hikari.hikari.test.data.model.SummaryStandardResult;
import org.springframework.data.jpa.repository.JpaRepository;

public interface SummaryStandardResultDAO extends JpaRepository<SummaryStandardResult,Integer> {
}
